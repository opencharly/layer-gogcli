# 2026.246.1816 — fix: gogcli module path (steipete -> openclaw)

## Summary

The upstream `gog` module renamed from `github.com/steipete/gogcli` to
`github.com/openclaw/gogcli`. The candy's `go install` build step still used the
old path, so `go install github.com/steipete/gogcli/cmd/gog@latest` failed with a
module-path mismatch (`module declares its path as: github.com/openclaw/gogcli`).
This broke every box composing `layer-gogcli` (the openclaw-full metalayer among
them) at build time.

Fix: the build step and description now use `github.com/openclaw/gogcli`. The
candy's own CalVer moves because changed content must ship under a new version.

## How tested

### 1. `charly box validate` (the config gate)

```
$ /tmp/charly-ci/bin/charly box validate
exit 0
```

Run with the charly version this repo's own `deploy.yml` clones and builds
(v2026.238.1242).

### 2. The `check-openclaw-full-pod` bed run (the R10 proof — exercises the renamed `go install`)

The consuming bed builds the openclaw-full image (which composes this candy) and
runs the candy's plan checks. Run with `CHARLY_REPO_OVERRIDE` pointing
`layer-gogcli` at this PR's fixed checkout (the standard dev-tool for proving an
uncommitted candy change before it is tagged):

```
$ charly check run check-openclaw-full-pod
charly check run check-openclaw-full-pod: [image-build] PASS after 6m45.109s [warnings=2 allowlisted=6 cache=284/369]
charly check run check-openclaw-full-pod: [check-image] PASS after 45.544s [allowlisted=1]
charly check run check-openclaw-full-pod: [deploy-add] PASS after 7.173s [allowlisted=1]
charly check run check-openclaw-full-pod: [config] PASS after 6.627s [allowlisted=1]
charly check run check-openclaw-full-pod: [start] PASS after 3.696s [allowlisted=1]
charly check run check-openclaw-full-pod: [bring-up-members] PASS after 52ms
charly check run check-openclaw-full-pod: [check-live] PASS after 43.675s [allowlisted=1]
charly check run check-openclaw-full-pod: [feature-run] PASS after 33.431s [allowlisted=1]
charly check run check-openclaw-full-pod: [update] PASS after 21.503s [allowlisted=5]
charly check run check-openclaw-full-pod: [check-live-rebuild] PASS after 34.914s [allowlisted=1]
charly check run check-openclaw-full-pod: [feature-run-rebuild] PASS after 33.537s [allowlisted=1]
charly check run check-openclaw-full-pod: [cleanup] PASS after 7.001s [allowlisted=1]
charly check run check-openclaw-full-pod: [cleanup-members] PASS after 0s
charly check run check-openclaw-full-pod: PASS (steps=13)
```

The `check-live` step (207 steps: 178 passed, 0 failed, 29 skipped) includes the
gogcli plan checks proving the renamed `go install` produced a working binary:

```
PASS  check the gog binary is installed at ~/go/bin/gog  [plan:candy:github.com/opencharly/layer-gogcli:1]  ok
PASS  check running `gog --version` exits 0, proving the built binary actually runs  [plan:candy:github.com/opencharly/layer-gogcli:2]  exit=0
PASS  check `gog --help` lists the Google Workspace subcommands (gmail, calendar)  [plan:candy:github.com/opencharly/layer-gogcli:3]  exit=0
```

The image-build step (which executes the changed `go install
github.com/openclaw/gogcli/cmd/gog@latest`) PASSED after 6m45s — the renamed
module path resolves and builds. (The `run:` step itself is skipped in
verify-only mode; the binary it produces is asserted by the three PASS checks
above.)

**Warnings RCA (B5/R1):** the `check-openclaw-full-pod` image-build reports
`warnings=2` — both are routed to NAMED thematic batches with real fixes:

1. `warning: podman store is bloated (69.6GiB reclaimable, ~83% of 83.1GiB)` —
   a HOST condition (the podman store accumulated bed images; tracked in
   opencharly/charly#173). **Named batch: host hygiene — podman store reclaim.**
   The fix is in progress on this host: `podman image prune -a` is running and
   has already reduced the store from 113.9GB to 75.7GB (reclaimable 93.2GB →
   55.0GB). Once complete, the warning disappears from subsequent bed runs.
2. `warning: package chacha20 v0.10.1 in Cargo.lock is yanked in registry crates-io` —
   from the composed `layer-himalaya` candy's `cargo install himalaya --locked`
   (the `--locked` flag pins the yanked crate from the crate's Cargo.lock).
   **Named batch: layer-himalaya Cargo.lock refresh — opencharly/layer-himalaya#4**
   (same batch, drops `--locked` so cargo resolves fresh). The fix is a real
   change at the source, not a deferral.

## Rulebook compliance

- **R0 (skills)** - `/charly-image:layer` for the candy/plan model;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** - the failure was root-caused: the upstream module rename broke the
  `go install` path; the fix updates the path at the source.
- **R2 (no deferral)** - the fix ships now, unblocking the openclaw-full bed.
- **R3 (no duplication)** - the module path is named once in the build step.
- **R4 (no workarounds)** - the path is fixed, not pinned to an old commit.
- **R5 (hard cutover)** - the old path is gone; no old path remains.
- **R6 (git safety)** - feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** - `charly box validate` on the committed tree AND
  the `check-openclaw-full-pod` bed run (PASS, steps=13) with the verbatim output
  above, exercising the changed `go install` path.
- **R8 (generation)** - N/A: no generated artifact, no CUE input in this repo.
- **R9 (artifact integrity)** - N/A: no image is built here.
- **ADE (candy description/plan)** - `description:` and `plan:` updated in
  lockstep with the change they describe.
- **SDD (CUE source)** - N/A: no schema authored or generated here.

## Change classification

- **Change class:** candy build-step fix (module path). Config only.
- **Verification gate:** `charly box validate` + the `check-openclaw-full-pod` bed
  run (PASS, steps=13, verbatim output above)
- **Attribution tier:** `fully tested and validated` - the bed ran live on the
  final tree with the verbatim verdict recorded, exercising the changed path

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*